# Implementation Diary - 2026-05-09

## Status
- [x] Research UsersService and related entities
- [x] Create Fakers for User and Requests
- [x] Implement UsersServiceTests
- [x] Implement AuthenticatorServiceTests
- [x] Fix Dapper schema issues with SQLite ATTACH
- [x] Fix environment and WireMock consistency
- [x] Validate all tests (62/62 passing)

## Progress
- Solved SQLite `AUTOINCREMENT` errors by simplifying column types in `DbContextMockFactory`.
- Implemented `ATTACH DATABASE` in SQLite to support Dapper queries using `user_details` and `journey` schemas.
- Created `SyncToAttachedDatabasesAsync` to keep EF Core data consistent with Dapper views.
- Introduced `WireMockFixture` on port 8080 to match `.env` and avoid parallel test conflicts.
- Made `SmtpModel.ResetTokenMailAsync` virtual to enable proper mocking of email sending.
- Fixed `NullReferenceException` in `AuthDbCacheService` mock by initializing the `rnd` field.
- All 62 tests passed successfully.
